spring:
  application:
    name: monitoring
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URL}
      fail-fast: true
#      retry:
#        initial-interval: 2000 # Thời gian chờ ban đầu là 2 giây
#        max-attempts: 15       # Số lần thử lại tối đa
#        max-interval: 5000
